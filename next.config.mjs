/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.ibb.co",
      "images.unsplash.com",
      "plus.unsplash.com",
      "srv863654.hstgr.cloud",
      "api.aaryansourcing.com",
      "localhost"
    ],
  },
};

export default nextConfig;